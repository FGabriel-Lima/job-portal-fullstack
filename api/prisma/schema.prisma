generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// Tabela de Administradores
model Admin {
  id        String   @id @default(uuid())
  name      String
  email     String   @unique
  password  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("admins")
}

// Tabela de Vagas
model Job {
  id          String   @id @default(uuid())
  title       String
  department  String
  location    String
  type        String   // Ex: CLT, PJ, Estágio
  salary      String
  description String   @db.Text
  requirements String  @db.Text
  benefits    String   @db.Text
  process     String   @db.Text
  applyLink   String
  status      String   @default("Aberta") // "Aberta" ou "Encerrada"
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("jobs")
}